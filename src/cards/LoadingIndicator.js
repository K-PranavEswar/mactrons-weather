import React from 'react';

// This is a simple placeholder. You can replace this with a more complex SVG or CSS animation.
const LoadingIndicator = () => (
    <div className="loading-indicator">
        <div className="gemini-pulse"></div>
    </div>
);

export default LoadingIndicator;